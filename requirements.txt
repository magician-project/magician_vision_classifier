# Core: training, inference and analysis
numpy
opencv-python
pillow
torch
torchvision
torchmetrics
pytorch-lightning
h5py
tqdm
psutil

# Metrics and plotting
scikit-learn
matplotlib
seaborn

# Experiment logging (TensorBoard is the default; wandb is opt-in via config)
tensorboard
wandb

# Optional: 3D detection viewer (integrator3D.py). Also needs system freeglut,
# e.g. sudo apt install freeglut3-dev
PyOpenGL
